# Cross-Silo-Federated-Learning-System
The proposed system introduces a machine learning-based vulnerability detection tool that integrates ensemble learning algorithms (SVM, KNN, and Naïve Bayes) for improved classification performance. The core advantage lies in the model’s ability to learn from historical data, enabling the prediction of new and unseen attack patterns.
